package tests;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
//@Suite.SuiteClasses({LoginTest.class, RegistrationTest.class})
public class TestSuiteTraining {

	//Esta clase permanece vacia, se usa solo como titular para las anotaciones anteriores
	//Dar click derecho dentro de esta clase y click en run as -> Junit test
}
